import { Button } from "@/components/ui/button";
const App = () => {
  return (
    <div>
      <Button className="text-bold text-3xl text-blue-500">App</Button>
    </div>
  );
};

export default App;
